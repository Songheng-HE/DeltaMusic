# 贡献曲目与代码

欢迎贡献，但请优先保证安全、可复现和版权清晰。

## 分享 MIDI（推荐）

最适合普通用户的分享方式是单独提供 `.mid` 或 `.midi` 文件。用户可在 DeltaMusic 中点击“选择 MIDI（可多选）”，或将 MIDI 拖入投放区域；启动器会以默认安全播放器导入，不会执行外部代码。

- 英文文件名会生成英文曲目 ID；中文文件名会生成拼音 ID。
- 同 ID 已存在时，启动器会自动添加数字后缀。
- 导入后用户可右键重命名显示名称。

请尽量提供单旋律 MIDI。多轨编曲、和弦或复杂伴奏可能不适合默认安全播放器。

## 分享 Python + MIDI（高级）

`.py` 是可执行代码，不能被启动器自动认定为安全。只有在确实需要自定义逻辑时才提交：

1. 不要写死个人电脑的绝对路径。
2. 不要在导入或启动时下载、执行或修改无关文件。
3. 明确说明需要的 MIDI 文件、键位映射、音域和停止逻辑。
4. 支持 F10 停止，或在说明中明确不支持。
5. 不要要求用户为了运行曲目关闭 Windows、浏览器或游戏安全机制。

## 提交前检查

- 使用一台普通 Windows 用户环境实际测试。
- 为新功能补充或更新自动化测试。
- 更新 `CHANGELOG.md` 的 `Unreleased` 条目。
- 不提交 `build_exe/`、`release*/`、个人曲库、日志、证书、密钥或 `.env` 文件。

## MIDI 与版权

代码的 MIT 许可证不自动覆盖 MIDI、歌曲名称、编曲、乐谱或原作。请只提交你拥有分发权、属于公共领域或具有明确开放许可的 MIDI；保留来源和授权信息。若没有重新分发权，请只提交制作方法或合法获取 MIDI 的链接。
